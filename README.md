# lemonlovesleepinglolxp

這是一個純靜態的翻牌記憶小遊戲網站，包含：

- `index.html`
- `style.css`
- `script.js`

## GitHub Pages 部署

這個倉庫已經配置了 GitHub Actions，自動部署到 GitHub Pages。

部署完成後，網站應該會在：

`https://lemon-chan-bit.github.io/lemonlovesleepinglolxp/`

如果你想手動觸發部署，只要把更改推到 `main` 分支即可。